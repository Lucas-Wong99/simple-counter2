import React from 'react';

import './button.css';

const MinusFive = (props) => {
  return (
      <button className="minusfive" onClick={props.minusFive}>-5</button>
  )
}

export default MinusFive;
